[project]
name = "gaiji-converter"
version = "0.1.0"
description = "Prototype gaiji to Unicode conversion toolkit"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
web = ["flask>=2.2"]

[tool.pytest.ini_options]
addopts = "-q"
